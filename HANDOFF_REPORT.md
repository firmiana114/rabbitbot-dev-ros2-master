# 交接报告

## 背景和目标

本轮目标是按 Aaron 提供的新剧本点位和中转点，更新当前 `June6_workflow` 分支中的 DOCX 剧本导航点位配置，使点位/中转点与六月六日导览 workflow 对齐。项目主机 `AGX-orin-FX`，路径 `/mnt/ssd/navgation/projects/rabbitbot-dev-ros2-master`，分支 `June6_workflow`。

## 当前状态

已完成：

- 已从本机 Codex 缓存读取六月六日剧本：`/Users/firmiana/Documents/Codex/2026-06-01/files-mentioned-by-the-user-0605/outputs/机器人小星剧本提取.md`。
- 已更新 `rabbitbot/agno_agents/workflow.py` 中的 `DOCX_SCRIPT_POINTS`：
  - `点位1` 更新为新“起点”坐标。
  - `点位2` 更新为 `1->2过渡点位` + `2点位` 两段导航点。
  - `点位3` 更新为新 `点位3` 坐标。
  - `点位4` 更新为新 `点位4` 坐标，并标注为 3->5 路径中转点。
  - `点位5` 更新为 `点位4中转` + `4->5过渡点位` 两段路径，用于“前往点位5并指引小巴方向”步骤。
- 已保留 `z` 和 `note` 字段用于人工核对；导航实际读取 `x/y/ox/oy/oz/ow`。

未完成：

- Aaron 提供的截图中没有单独的“点位5终点”坐标，只有 `4->5过渡点位`；本轮先将该点作为当前点位5路径的最终可达点。
- 尚未在真机/完整 workflow 中验证导航能否按新点位成功到达。

## 已验证的事实

- 当前剧本步骤仍是：
  - 起点/点位1：开场欢迎。
  - 点位2：点咖啡。
  - 点位2->3：边走边做初步介绍。
  - 点位3：拿取咖啡和补充介绍。
  - 点位3->5：经点位4和4->5过渡点前往告别/小巴指引区域。
- `_extract_location_points` 会忽略 `z/mode/note`，只将 `x/y/ox/oy/oz/ow` 传给导航接口。
- `rabbitbot/agno_agents/workflow.py` 已通过 Python 编译检查。

## 阻塞问题

无代码层面的阻塞。运行层面仍需确认“4->5过渡点位”是否就是现场期望的点位5最终停靠点；如果不是，需要补充真实点位5终点坐标。

## 建议的下一步

- 先跑非联调 workflow，确认点位步骤顺序与剧本文案一致。
- 再跑联调 workflow，重点观察：
  - 点位1到点位2是否经过 `1->2过渡点位`。
  - 点位3到点位5是否经点位4和 `4->5过渡点位`。
  - 到达“告别并指引小巴方向”时机器人朝向是否符合现场预期。
- 若现场还有点位5终点坐标，应将其追加到 `点位5.location` 的最后一项，而不是替换点位4中转。

## 注意事项

- 本轮只改 DOCX 剧本点位配置和交接报告，未改剧本文案、动作字段、STT 灵敏度或 unified 启动脚本。
- `DOCX_SCRIPT_POINT_ENTITY` 仍保持 `point_2 -> 点位2`、`point_3 -> 点位3`、`point_4 -> 点位4`、`point_5 -> 点位5`。
- 当前 `点位4` 本身没有单独剧本文案步骤，但会作为点位3前往点位5的中转点使用。

## 其它信息

如需同步到 `feature/unified-runtime-image`，可 cherry-pick 本轮提交。
