# 2026-04-27 变更记录

## 总览

- 本次目标是让 `rabbitbot-dev-ros2-master` 中的 TTS/STT 服务先跑通，再从无音频设备模式升级到有音频设备模式，并让 STT 使用 CUDA 版 CTranslate2。
- TTS 侧已完成：启动脚本改用 `/opt/venv`；补齐 `kokoro.KPipeline` 依赖；改为优先读取本地 `/data/models/kokoro/Kokoro-82M` 模型；支持无输出设备模式；在有设备容器中可自动选择输出设备并监听 `28185`。
- STT 侧已完成：补齐 faster-whisper 本地模型 `/data/models/faster-whisper/Systran/faster-whisper-base`；绕过 onnxruntime/openwakeword 导入崩溃；补 Silero VAD 离线缓存；支持无输入设备模式；在有设备容器中可自动选择输入设备并监听 `28184`。
- 音频设备问题已定位并处理：宿主机有 `/dev/snd` 和 Orin HDA/APE 音频端点，原容器没有挂载音频设备；新容器通过 `--device /dev/snd:/dev/snd --group-add audio -v /usr/share/alsa:/usr/share/alsa:ro` 进入有设备模式。
- TTS/STT 启动脚本已从固定查找 `UACDemo` / `USB PnP Audio Device` 调整为：优先按设备名匹配，找不到时自动 fallback 到第一个可用输出/输入通道；当前 TTS 验证选择 HDMI 输出，STT 验证选择 Orin APE 输入端点。
- CTranslate2 已在新音频容器内源码编译为 CUDA 版，安装到 `/opt/ctranslate2-cuda`，Python 绑定安装到 `/opt/venv`；验证 `ctranslate2.get_cuda_device_count()` 返回 `1`，`faster_whisper` 可用 `device="cuda", compute_type="float16"` 加载本地模型。
- STT 启动脚本已默认导出 CUDA 版运行库路径，并默认使用 `STT_DEVICE=cuda`、`STT_COMPUTE_TYPE=float16`；验证日志显示 `stt_device: cuda, stt_compute_type: float16`。
- 已固化两个镜像：中间镜像 `navid-rabbitbot:stt-tts-fixed-20260427`，最终镜像 `navid-rabbitbot:stt-tts-audio-ct2cuda-20260427`；推荐后续使用最终镜像重建容器。
- 推荐容器名为 `navid-vllm-cuda-mic-audio`。重建容器后先启动 TTS，再启动 STT；当前验证命令为 `bash scripts/start_tts_app.bash` 和 `bash scripts/start_stt_app.bash`。
- 创建容器命令：

```bash
docker run -d \
  --name navid-vllm-cuda-mic-audio \
  --runtime nvidia \
  --network host \
  --device /dev/snd:/dev/snd \
  --group-add audio \
  -v /usr/share/alsa:/usr/share/alsa:ro \
  -v /mnt/ssd/navgation/projects:/data \
  navid-rabbitbot:stt-tts-audio-ct2cuda-20260427 \
  tail -f /dev/null
```

- 进入容器命令：

```bash
docker exec -it navid-vllm-cuda-mic-audio bash
```

- 前台启动服务命令，先启动 TTS，再另开终端启动 STT：

```bash
cd /data/rabbitbot-dev-ros2-master
bash scripts/start_tts_app.bash
```

```bash
cd /data/rabbitbot-dev-ros2-master
bash scripts/start_stt_app.bash
```

- 后台启动两个服务命令：

```bash
docker exec navid-vllm-cuda-mic-audio bash -lc 'cd /data/rabbitbot-dev-ros2-master && nohup bash scripts/start_tts_app.bash >/tmp/rabbitbot_tts.log 2>&1 &'
docker exec navid-vllm-cuda-mic-audio bash -lc 'cd /data/rabbitbot-dev-ros2-master && nohup bash scripts/start_stt_app.bash >/tmp/rabbitbot_stt.log 2>&1 &'
```
- 当前遗留风险：真实麦克风链路和真实扬声器播放只验证到设备枚举和服务启动，未做现场收音/放音质量测试；ALSA 仍会输出部分默认 PCM/JACK 警告，但不阻塞服务启动。

## TTS 启动脚本改为使用 /opt/venv

- 文件：scripts/start_tts_app.bash
- 变更：将虚拟环境入口从 source py310/bin/activate 改为 source /opt/venv/bin/activate。
- 原因：容器内不存在 /data/rabbitbot-dev-ros2-master/py310，现有 STT/TTS 依赖主要安装在 /opt/venv。
- 验证：重新运行 bash scripts/start_tts_app.bash 后，py310 缺失问题消失；当前阻塞变为容器未枚举到 UACDemo 音频输出设备，导致 OUTPUT_DEVICE_INDEX 为空。
- 后续阻塞：绕过音频设备后，TTS 仍需处理 KPipeline 未定义以及 kokoro 依赖不匹配问题。

## docs 文档阅读结论

- 机器人启动文档说明语音模块预期在 navid-vllm-cuda-mic 容器中启动，顺序为先 TTS 后 STT。旧路径为 /datanvme/fuchengjia/projects/rabbitbot-dev-ros2-dev，当前实际路径应替换为 /data/rabbitbot-dev-ros2-master。
- 机器人音频文档说明容器内应能通过 scripts/start_pyaudio_test.bash 枚举音频设备；示例中 USB 音响 CS201 对应 TTS，USB Composite Device 麦克风对应 STT。
- 音频文档建议可以直接在脚本中写固定设备编号，例如 STT 的 INPUT_DEVICE_INDEX 和 TTS 的 OUTPUT_DEVICE_INDEX；但当前容器未挂载音频设备，枚举不到任何可用设备。
- 启动文档还说明 STT、TTS 正常情况下不需要频繁重启；修改 workflow.py 后需要重启 workflow，修改 kuavo_robot_ros 后需要重启 robot_app。
- 其它文档主要覆盖 VLM、YOLO、VLN、机器人网络拓扑和测试流程，没有提供 py310 自动安装或 STT/TTS 依赖重建流程。

## 音频设备排查结论

- 主机 /dev/snd 存在，aplay/arecord 能看到 NVIDIA Jetson AGX Orin HDA/APE 相关设备。
- 主机未看到 docs 中提到的 USB 音响 CS201、UACDemo、USB PnP Audio Device 或 USB Composite Device。lsusb 只看到 USB Hub，没有目标 USB 音频设备。
- 主机 PulseAudio 当前连接失败，pactl list short sinks/sources 返回 Connection refused。
- 容器内没有 /dev/snd，aplay/arecord 命令也不存在；PyAudio 枚举只输出 ALSA/JACK 错误，未列出任何设备。
- docker inspect 显示容器 Privileged=false、Devices=[]，当前没有把主机音频设备传入容器。
- 结论：当前音频阻塞分两层：目标 USB 音频硬件在主机侧未识别；即便主机识别后，也需要重新创建或调整容器参数，把 /dev/snd 和音频权限传入容器。

## TTS 无输出设备模式

- 文件：scripts/start_tts_app.bash
- 变更：当脚本找不到 UACDemo 输出设备时，不再导出空的 OUTPUT_DEVICE_INDEX，而是 unset OUTPUT_DEVICE_INDEX，并提示以无输出设备模式启动。
- 文件：tts_app.py
- 变更：OUTPUT_DEVICE_INDEX 为空字符串或未设置时解析为 None，避免 int("") 报错。
- 文件：rabbitbot/audio/run_tts_espnet.py
- 变更：device_id 为 None 时不创建 sounddevice 输出流，WAV worker 跳过实际播放并将对应 tts_index 标记为已播放；stop、sound、sound_wav 也兼容无 sd_stream。
- 验证：运行 bash scripts/start_tts_app.bash 后，日志显示 未找到输出音频设备 UACDemo，将以无输出设备模式启动 TTS 和 out_device_id: None，说明音频设备空索引问题已绕过。
- 当前阻塞：TTS 继续失败在 KPipeline 未定义，即 rabbitbot/audio/run_tts_espnet.py 中调用 KPipeline(lang_code="z", device="cuda")，但当前代码未导入 KPipeline，且 kokoro 依赖 API 需要继续核对。
- 测试日志：容器内 /tmp/rabbitbot_tts_no_device_mode.log。

## Cloud TTS 方案验证

- 目标：通过 kuavo_configs.json 中 edge_tts_host_url 配置云端 TTS，绕过本地 KPipeline。
- 已测试地址：http://10.10.30.22:28187/tts，主机和容器内访问均超时。
- 额外测试候选地址：http://10.10.30.12:28187/tts、http://10.10.30.19:28187/tts、http://10.10.30.22:28187/tts，容器内均超时。
- 结论：方案 1 当前被外部 cloud TTS 服务不可达阻塞；未修改 kuavo_configs.json，避免把 edge_tts_host_url 设置为不可达地址导致 TTS 启动阶段反复等待请求超时。
- 下一步：需要先在边缘台式机启动 docs 中的 tools/run_cloud_tts_app.sh，或确认实际可达的 cloud TTS IP/端口；服务可达后再设置 edge_tts_host_url 并复测。

## Kokoro KPipeline 方案实施

- 开始实施方案 2：安装并接入提供 KPipeline 的 PyPI kokoro 包。
- 已计划先备份 /opt/venv pip freeze 到容器 /tmp/rabbitbot_pip_freeze_before_kokoro.txt，并检查 torch/espeak-ng，避免破坏 Jetson CUDA/PyTorch 环境。

- pip dry-run 使用默认 Jetson AI Lab 源失败：容器无法解析 pypi.jetson-ai-lab.dev，未安装任何包。
- 改用官方 PyPI 并优先 --no-deps dry-run，以避免替换现有 torch 2.6.0 CUDA 12.8 环境。
- 已安装 kokoro-0.9.4 本体：pip install --no-deps -i https://pypi.org/simple "kokoro>=0.9.4"。
- 导入测试当前失败在缺少 loguru，符合 --no-deps 安装后的预期；下一步按 kokoro 元数据补缺失的非 torch 依赖。

- 导入测试：source /opt/venv/bin/activate 后执行 from kokoro import KPipeline 成功，输出为 <class 'kokoro.pipeline.KPipeline'>。

- 代码改动：rabbitbot/audio/run_tts_espnet.py 已改为显式 from kokoro import KPipeline，并移除旧的延迟导入占位逻辑。

- 语法检查：tts_app.py 与 rabbitbot/audio/run_tts_espnet.py 通过 ast.parse 检查。
- 最小生成测试：KPipeline(lang_code="z", device="cuda") 已进入 Kokoro 初始化，不再出现 NameError；当前失败在 HuggingFace 模型下载，访问 https://huggingface.co/hexgrad/Kokoro-82M/resolve/main/config.json 报 Network is unreachable，本地缓存也没有对应 config.json。当前阻塞是模型资源无法下载/未预置，不是 KPipeline 导入问题。

- 服务启动测试：cd /data/rabbitbot-dev-ros2-master && bash scripts/start_tts_app.bash 已越过 KPipeline NameError，日志显示进入 EspnetTTS 初始化并调用 KPipeline(lang_code="z", device="cuda")。当前 uvicorn 未能保持监听 28185，退出原因是 HuggingFace 模型文件 hexgrad/Kokoro-82M/config.json 无法下载，启动脚本环境下请求 hf-mirror.com 超时，最终 LocalEntryNotFoundError。由于服务未成功监听，POST /exec 接口测试暂不能执行。

- espeak-ng 检查与安装：容器内原本未安装 espeak-ng；apt-get update 被 ROS2 noble 源缺失公钥 F42ED6FBAB17C654 阻塞，但直接 apt-get install -y espeak-ng 使用现有索引安装成功，安装版本为 1.51+dfsg-12build1。

- HuggingFace 缓存检查：容器内 HF_HOME/HUGGINGFACE_HUB_CACHE/TRANSFORMERS_CACHE 指向 /data/models/huggingface；该目录当前只看到 modules/__init__.py，没有 hexgrad/Kokoro-82M 的 config.json 或权重文件。因此当前需要让容器能访问 HuggingFace/hf-mirror，或提前把 hexgrad/Kokoro-82M 模型文件放进该缓存目录。
- 当前结论：方案 2 的 Python 包接入已经完成，NameError: KPipeline is not defined 已解决；TTS 新阻塞是 Kokoro 模型资源下载/缓存缺失。POST /exec 尚未测试，因为服务初始化阶段退出，28185 未监听。

- 依赖安装明细：已先执行 pip freeze > /tmp/rabbitbot_pip_freeze_before_kokoro.txt 备份安装前环境；随后安装 kokoro-0.9.4 本体，再按缺失模块安装 loguru 与 misaki[en]>=0.9.4。安装后 torch 校验为 torch=2.6.0、CUDA=12.8、torch.cuda.is_available()=True，未替换 Jetson CUDA/PyTorch 环境。

- 代码改动：rabbitbot/audio/run_tts_espnet.py 的 Kokoro 初始化已改为优先读取 /data/models/kokoro/Kokoro-82M 下的本地 config.json、kokoro-v1_0.pth 和 voices/zm_yunxi.pt；本地文件齐全时不再触发 HuggingFace 下载。

- 记录修正：上一条本地 Kokoro 初始化代码改动尝试因目标代码片段未匹配，实际未修改；下面重新按实际文件内容修改。

- 代码改动：rabbitbot/audio/run_tts_espnet.py 已实际修改为 from kokoro import KPipeline, KModel，并优先读取 /data/models/kokoro/Kokoro-82M 下的本地 config.json、kokoro-v1_0.pth 和 voices/zm_yunxi.pt；本地文件齐全时不再触发 HuggingFace 下载。

- 依赖补充：KPipeline(lang_code="z", model=False) 暴露中文 G2P 缺失 ordered_set；已 dry-run misaki[zh]>=0.9.4，确认只会安装 cn2an、jieba、ordered-set、proces、pypinyin-dict，不涉及 torch；随后已安装这些依赖。
- 模型文件处理：远端主机和容器均可访问 PyPI，但访问 huggingface.co 与 hf-mirror.com 超时；本机可访问 HuggingFace，因此已从本机下载 hexgrad/Kokoro-82M 的 config.json、kokoro-v1_0.pth、voices/zm_yunxi.pt，并复制到 /mnt/ssd/navgation/projects/models/kokoro/Kokoro-82M，容器内对应 /data/models/kokoro/Kokoro-82M。

- 依赖补充：离线最小生成测试中 misaki.zh 导入失败，原因是 pypinyin 0.44.0 缺少 tone_convert.to_finals；已 dry-run 并升级 pypinyin 到 0.55.0，不涉及 torch。

- 最小离线生成测试：使用 /data/models/kokoro/Kokoro-82M 的本地 config、kokoro-v1_0.pth、voices/zm_yunxi.pt 初始化 KModel/KPipeline 成功；输入“你好”生成 torch.float32 音频 tensor，shape 为 [44400]，未再访问 HuggingFace。
- 服务启动测试：cd /data/rabbitbot-dev-ros2-master && bash scripts/start_tts_app.bash 已成功启动；无 UACDemo 输出设备时进入无输出设备模式，日志显示 Application startup complete，容器内 127.0.0.1:28185 端口已打开。

- 接口测试：POST http://127.0.0.1:28185/exec，表单 task={"task":"text_to_speech","lang":"zh","text":"你好","timeout":30} 返回 {"out_text":"4"}；日志显示 KokoroTTS: text 你好, time 0.498。
- 无输出设备队列验证：随后调用 get_play，task={"task":"get_play","lang":"zh","text":"4","timeout":30} 返回 {"out_text":"1.0"}，说明无输出设备模式下该 TTS index 已标记为完成。
- 当前 TTS 状态：容器内 scripts/start_tts_app.bash 已可启动 TTS 服务，uvicorn 监听 28185；真实播放仍依赖后续把主机音频设备挂进容器并设置 OUTPUT_DEVICE_INDEX。

## STT 阻塞修复实施

- 开始按顺序处理 STT：先解决 onnxruntime/openwakeword 导入崩溃，再补 Silero VAD 离线缓存，最后补 start_stt_app.bash 的空 INPUT_DEVICE_INDEX 处理。

- onnxruntime/openwakeword 修复：已备份容器内 /opt/venv/lib/python3.12/site-packages/RealtimeSTT/audio_recorder.py 到 /tmp/audio_recorder.py.before_openwakeword_lazy_20260427；随后将 openwakeword 顶层导入改为懒加载，仅在 wake_words 且 wakeword_backend 为 openwakeword/oww 时才导入，当前业务未启用 wake word 时不再触发 onnxruntime。

- onnxruntime/openwakeword 修复验证：首次懒加载 patch 后已绕过 onnxruntime 原生 abort，但 RealtimeSTT 导入继续暴露 pvporcupine 在 Jetson 上 Unsupported platform；已同样将 pvporcupine 改为仅在 Porcupine wake word 启用时懒加载。当前 from RealtimeSTT import AudioToTextRecorder 已成功。

- Silero VAD 离线缓存：本机下载 snakers4/silero-vad master 分支，放入容器可见的 /data/models/torch/hub/snakers4_silero-vad_master；使用 TORCH_HUB_DISABLE_NETWORK=1 执行 torch.hub.load("snakers4/silero-vad", "silero_vad", onnx=False) 成功，返回 TorchScript RecursiveScriptModule，未触发 onnxruntime。

- STT 启动脚本空输入设备处理：scripts/start_stt_app.bash 已改为找不到 USB PnP Audio Device 时 unset INPUT_DEVICE_INDEX，并提示以无输入设备模式启动 STT，避免导出空字符串。

- STT 启动验证：执行 scripts/start_stt_app.bash 后，RealtimeSTT 导入不再触发 onnxruntime/openwakeword/pvporcupine 崩溃；Silero VAD 也不再报 torch hub 缓存缺失。当前服务仍未监听 28184，新的阻塞为 faster-whisper base 模型未缓存，尝试访问 hf-mirror.com/Systran/faster-whisper-base 时因代理 127.0.0.1:7897 拒绝连接而失败，随后 LocalEntryNotFoundError；同时音频线程仍反复提示 No input devices found。已停止本次测试启动的 STT 后台进程。
- 当前 STT 剩余阻塞：1）需要离线预置 Systran/faster-whisper-base 或改为本地 download_root；2）需要把输入音频设备挂入容器，或继续做无输入设备下的服务降级启动。

- 进程清理：本次 STT 启动验证后残留的 uvicorn stt_app:app 测试进程已停止。

## STT faster-whisper base 离线模型处理

- 开始处理 Systran/faster-whisper-base 未缓存问题：计划在本机下载模型 snapshot，复制到容器可见目录，并让 stt_app.py 优先使用本地路径，避免启动时访问 hf-mirror.com。

- faster-whisper base 模型预置：本机使用 huggingface_hub 下载 Systran/faster-whisper-base，复制到 /mnt/ssd/navgation/projects/models/faster-whisper/Systran/faster-whisper-base，容器内对应 /data/models/faster-whisper/Systran/faster-whisper-base。包含 config.json、model.bin、tokenizer.json、vocabulary.txt。
- stt_app.py 改动：STT 模型现在优先使用环境变量 STT_MODEL_PATH，默认本地路径 /data/models/faster-whisper/Systran/faster-whisper-base；本地路径存在时传给 AudioToTextRecorder，避免 faster-whisper 启动时访问 hf-mirror.com/Systran/faster-whisper-base。语法检查通过。

- faster-whisper 本地模型加载验证：模型路径已生效，不再报 Systran/faster-whisper-base 未缓存或访问 hf-mirror.com；当前继续前进到新的运行时问题：WhisperModel(..., device="cuda") 报 This CTranslate2 package was not compiled with CUDA support。

- CTranslate2 设备策略：本地 faster-whisper 模型加载已不再访问 hf-mirror.com，但当前 /opt/venv 的 ctranslate2 4.7.1 报 This CTranslate2 package was not compiled with CUDA support。验证 WhisperModel(本地路径, device="cpu", compute_type="int8") 可加载成功。因此 stt_app.py 已增加 STT_DEVICE/STT_COMPUTE_TYPE 环境变量，默认使用 cpu/int8；后续如果安装 CUDA 版 CTranslate2，可通过环境变量切回 cuda/default。

- faster-whisper VAD filter 修正：启动验证发现 faster_whisper 自带 vad_filter 会再次导入 onnxruntime 并触发同样 native abort；stt_app.py 已显式设置 faster_whisper_vad_filter=False，避免转写模型内部走 onnxruntime VAD，保留 RealtimeSTT 外层已缓存的 Silero TorchScript VAD。

- RealtimeSTT warmup VAD 修正：关闭 faster_whisper_vad_filter 后，正式转写已传 vad_filter=False，但模型预热 transcribe 未传该参数，仍触发 faster-whisper 内置 onnxruntime VAD；已 patch /opt/venv/lib/python3.12/site-packages/RealtimeSTT/audio_recorder.py，让 warmup transcribe 也使用 self.faster_whisper_vad_filter。

## 2026-04-27 STT faster-whisper 离线模型修复结果
- 已将 Systran/faster-whisper-base 离线模型预置到宿主机 /mnt/ssd/navgation/projects/models/faster-whisper/Systran/faster-whisper-base，容器内路径为 /data/models/faster-whisper/Systran/faster-whisper-base。
- 已修改 stt_app.py：默认优先使用 STT_MODEL_PATH 或 /data/models/faster-whisper/Systran/faster-whisper-base，不再依赖运行时访问 hf-mirror.com；当前 CTranslate2 包不支持 CUDA，因此默认 STT_DEVICE=cpu、STT_COMPUTE_TYPE=int8。
- 已修正 RealtimeSTT 预热转写路径，让 faster-whisper warmup 也遵守 faster_whisper_vad_filter=False，避免预热阶段再次触发 onnxruntime VAD 导入崩溃。
- 验证结果：bash scripts/start_stt_app.bash 已成功启动，容器内 uvicorn stt_app:app 正在监听 28184，日志显示使用本地 faster-whisper 模型并完成初始化。
- 当前剩余阻塞：容器仍没有输入音频设备，日志会持续出现 No input devices found；真实语音识别需要把麦克风/ALSA 设备挂入容器。该问题与 faster-whisper 模型缓存已无关。

## 2026-04-27 STT 无输入设备模式
- 修改 stt_app.py：当 INPUT_DEVICE_INDEX 为空时，不再初始化 AudioToTextRecorder，改用 NoInputRecorder 占位对象；服务仍可启动，接口保持兼容，真实录音请求在无设备时返回空识别结果。
- 修改 scripts/start_stt_app.bash：当容器内不存在 /dev/snd 时，直接跳过 PyAudio 输入设备扫描，避免启动阶段输出大量 ALSA 设备错误。
- 验证结果：容器内 bash scripts/start_stt_app.bash 成功启动，uvicorn stt_app:app 监听 28184；POST /exec get_status_async 返回 {"out_text":"<REC_STOP>"}；带 text 的 speech_to_text 返回原文本。
- 当前状态：STT 服务无输入设备也能跑通；真实麦克风识别仍需要后续把输入音频设备挂入容器。

## 2026-04-27 有音频设备模式与 CTranslate2 CUDA 版
- 新建并验证音频容器：`navid-vllm-cuda-mic-audio`，基于已修复环境镜像创建，启动参数包含 `--runtime nvidia --network host --device /dev/snd:/dev/snd --group-add audio -v /usr/share/alsa:/usr/share/alsa:ro -v /mnt/ssd/navgation/projects:/data`。
- 宿主机 `/dev/snd` 已成功进入容器，PyAudio 可枚举到 `NVIDIA Jetson AGX Orin HDA` HDMI 输出和 `NVIDIA Jetson AGX Orin APE` 输入/输出端点。
- 修改 `scripts/start_tts_app.bash`：优先按 `TTS_DEVICE_NAME`/默认 `UACDemo` 查找输出设备，找不到时自动选择第一个 `maxOutputChannels > 0` 的输出设备；当前验证选择 `NVIDIA Jetson AGX Orin HDA: HDMI 0`。
- 修改 `scripts/start_stt_app.bash`：优先按 `STT_DEVICE_NAME`/默认 `USB PnP Audio Device` 查找输入设备，找不到时自动选择第一个 `maxInputChannels > 0` 的输入设备；当前验证选择 `NVIDIA Jetson AGX Orin APE` 输入端点。
- 修改 `tests/audio/test_pyaudio.py`：设备枚举输出同时显示 `max input channels` 和 `max output channels`，方便判断输入/输出设备。
- 修改 `stt_app.py`：真实 `AudioToTextRecorder` 不支持 `set_language` 时跳过该调用，避免有设备模式接口报错。
- CTranslate2 CUDA 处理：在新音频容器内源码编译并安装 CTranslate2 4.7.1 到 `/opt/ctranslate2-cuda`，Python 绑定重装到 `/opt/venv`；编译使用 CUDA 12.8、cuDNN、Orin `sm_87`。
- CTranslate2 构建补丁：改用 CUDA 12.8 自带 Thrust/CUB，给 CTranslate2 源码补充 CUDA 12.8 所需 Thrust 显式 include，并修复 NEON 头文件路径；构建日志保存在 `/data/build_logs/ctranslate2_cuda_*_20260427.log`。
- 修改 `scripts/start_stt_app.bash`：默认导出 `LD_LIBRARY_PATH=/opt/ctranslate2-cuda/lib:...`、`STT_DEVICE=cuda`、`STT_COMPUTE_TYPE=float16`。
- 验证：`import ctranslate2` 后 `get_cuda_device_count()` 返回 `1`，`get_supported_compute_types("cuda")` 返回包含 `float16/int8_float16/float32` 等类型；`faster_whisper.WhisperModel(..., device="cuda", compute_type="float16")` 成功加载本地模型。
- 服务验证：新音频容器中 TTS 监听 `28185`，STT 监听 `28184`；STT 日志显示 `stt_device: cuda, stt_compute_type: float16`，`POST /exec get_status_async` 返回 `<REC_STOP>`。
- 已将新音频容器固化为镜像 `navid-rabbitbot:stt-tts-audio-ct2cuda-20260427`，当前正在运行的容器仍是 `navid-vllm-cuda-mic-audio`。

## 2026-04-28 现场音频设备与 STT 启动记录

- TTS 真实播放已验证成功；现场 USB 输出设备可通过 `TTS_DEVICE_NAME=BT67` 指定，启动日志应选中 BT67/USB Audio，而不是 HDMI。
- STT 现场麦克风设备枚举结果会随容器启动和设备插拔变化；无线麦克风曾显示为 `Wireless Mic Rx: USB Audio`，BT67 也会作为 USB Audio 设备出现。硬件直连设备通常只支持 `48000Hz`，而 RealtimeSTT 内部会先尝试 `16000Hz`，容易在硬件直连设备上出现 `paInvalidSampleRate`。
- 为避免 `scripts/start_stt_app.bash` 自动扫描并覆盖手动设置的 `INPUT_DEVICE_INDEX`，现场调试 STT 时先不要通过该脚本启动，而是手动导出环境变量后直接启动 `uvicorn stt_app:app`。
- 启动前需要先清理旧 STT 进程，避免 `28184` 端口被占用：

```bash
docker exec -it navid-vllm-cuda-mic-audio bash

pkill -9 -f "uvicorn stt_app:app"
pkill -9 -f "scripts/start_stt_app.bash"
ps -ef | grep stt_app | grep -v grep
```

- 先启动 TTS，STT 启动时会调用 TTS 播放“机器人语音输入模块加载完毕”，如果 `28185` 未监听，STT 会在导入阶段因连接 TTS 失败而退出：

```bash
docker exec -it navid-vllm-cuda-mic-audio bash
cd /data/rabbitbot-dev-ros2-master

export TTS_DEVICE_NAME=BT67
bash scripts/start_tts_app.bash
```

- 另开终端启动 STT。当前推荐先使用 ALSA 的 `sysdefault/default` 设备，让 ALSA 做重采样；现场枚举中 `sysdefault` 曾为 `26`、`default` 曾为 `28`，启动前应重新枚举确认：

```bash
docker exec -it navid-vllm-cuda-mic-audio bash

python -c 'import pyaudio; p=pyaudio.PyAudio();
for i in range(p.get_device_count()):
 d=p.get_device_info_by_index(i)
 if int(d.get("maxInputChannels",0))>0: print(i, d.get("name"), d.get("maxInputChannels"), d.get("defaultSampleRate"))
p.terminate()'
```

- 如果 `sysdefault` 的索引仍为 `26`，按以下方式手动启动 STT，不要再运行 `bash scripts/start_stt_app.bash`：

```bash
docker exec -it navid-vllm-cuda-mic-audio bash
cd /data/rabbitbot-dev-ros2-master

source /opt/venv/bin/activate

export LD_PRELOAD=/usr/lib/aarch64-linux-gnu/libgomp.so.1
export LD_LIBRARY_PATH=/opt/ctranslate2-cuda/lib:/usr/local/cuda/lib64:/usr/local/cuda/targets/aarch64-linux/lib:/usr/lib/aarch64-linux-gnu:${LD_LIBRARY_PATH}
export STT_DEVICE=cuda
export STT_COMPUTE_TYPE=float16
export INPUT_DEVICE_INDEX=26
export REALTIME_TTS_BASE_URL=http://localhost:28185/v1
export RABBITBOT_TTS_AGENT_URL=${REALTIME_TTS_BASE_URL}
export TORCH_HUB_DISABLE_NETWORK=1

uvicorn stt_app:app --host 0.0.0.0 --port 28184 --log-level debug --workers 1
```

- 正常启动的关键标志是日志出现 `in_device_id: 26`，并最终出现 `Uvicorn running on http://0.0.0.0:28184`。如果卡在 RealtimeSTT 初始化或反复出现 `Invalid sample rate. Retrying...`，说明当前设备索引不合适，需要重新枚举并优先尝试 `sysdefault/default`，而不是硬件直连的 `Wireless Mic Rx` 或 `BT67`。
- STT 接口不是持续常驻打印模式；`speech_to_text` 每次只识别一句话，说完一句就返回。如果需要连续监听，可以在测试终端循环调用：

```bash
docker exec -it navid-vllm-cuda-mic-audio bash

while true; do
  curl -s -X POST http://127.0.0.1:28184/exec \
    -F 'task={"task": "speech_to_text", "lang": "zh", "text": "", "timeout": 30}'
  echo
done
```

## 2026-04-28 VLM 服务启动记录

- `navid-rabbitbot:v1` 是 RabbitBot 应用镜像，不包含 `vllm` 命令；VLM 推理服务实际运行在 `vlm` 容器中，镜像为 `dustynv/vllm:0.9.2-r36.4-cu128-24.04`。
- 当前 `vlm` 容器使用 host 网络，模型目录挂载为 `/models`，宿主机对应路径是 `/mnt/ssd/navgation/projects/models`。
- 当前已验证可用的 VLM 模型路径是 `/models/Qwen2.5-VL-7B-Instruct-GPTQ-Int4`，对外服务模型名设置为 `Qwen2.5-VL-7B-Instruct`。
- 启动 VLM 服务命令：

```bash
docker start vlm

docker exec -d vlm bash -lc '
uv pip install pandas
vllm serve /models/Qwen2.5-VL-7B-Instruct-GPTQ-Int4 \
  --seed 42 \
  --gpu-memory-utilization 0.6 \
  --max-num-seqs 2 \
  --limit-mm-per-prompt "image=4,video=1" \
  --max-num-batched-tokens 2048 \
  --enable-chunked-prefill \
  --mm-processor-kwargs "{\"max_pixels\": 802816, \"fps\": 1}" \
  --max-model-len 65536 \
  --served-model-name Qwen2.5-VL-7B-Instruct \
  --port 8000 \
  > /models/qwen2.5-vl-7b-gptq.log 2>&1
'
```

- 启动后不要立刻判断失败。该模型会先加载权重、初始化 KV cache 并做 CUDA graph capture，现场验证从启动到 `8000` 端口可用约需 2-4 分钟。
- 查看启动日志：

```bash
docker exec -it vlm bash
tail -f /models/qwen2.5-vl-7b-gptq.log
```

- 正常启动成功标志：

```text
Starting vLLM API server 0 on http://0.0.0.0:8000
Application startup complete.
```

- 验证 VLM 服务：

```bash
curl http://127.0.0.1:8000/v1/models
```

- 现场验证返回模型信息如下，说明 VLM 已可被 RabbitBot 通过 `http://127.0.0.1:8000/v1` 调用：

```json
{
  "id": "Qwen2.5-VL-7B-Instruct",
  "root": "/models/Qwen2.5-VL-7B-Instruct-GPTQ-Int4",
  "max_model_len": 65536
}
```

- 如果 `curl http://127.0.0.1:8000/v1/models` 返回 `Connection refused`，先检查日志是否仍在加载模型或 CUDA graph capture；只要没有 traceback，通常继续等待即可。

## 2026-04-28 projectsjia 环境迁移到 projects

- 背景：`/mnt/ssd/navgation/projectsjia` 目录后续准备弃用，但当前 `start_kuavo_agno_workflow.bash` 依赖其中的 Python 3.10 环境、VLN ROS2 workspace 和 Orbbec SDK。为避免新容器继续挂载 `projectsjia`，已将必要环境复制到 `/mnt/ssd/navgation/projects`。
- 已将 `/mnt/ssd/navgation/projectsjia/rabbitbot-dev-ros2/py310` 复制到 `/mnt/ssd/navgation/projects/rabbitbot-dev-ros2-master/py310`。原 `/mnt/ssd/navgation/projects/rabbitbot-dev-ros2-master/py310` 已备份为 `py310.backup_20260428_135051`。
- 已将 `/mnt/ssd/navgation/projectsjia/pyorbbecsdk-v2-py310` 复制为 `/mnt/ssd/navgation/projects/pyorbbecsdk-v2-py310`。原来指向 `projectsjia` 的软链接已备份为 `pyorbbecsdk-v2-py310.backup_20260428_135051`。
- `/mnt/ssd/navgation/projects/vln` 已确认包含 `ros2_ws/install/setup.bash`，可满足脚本中的 `/datanvme/fuchengjia/projects/vln/ros2_ws/install/setup.bash` 路径。
- 新增 `/mnt/ssd/navgation/projects/rabbitbot-dev-ros2 -> rabbitbot-dev-ros2-master` 软链接，用于兼容复制过来的 `py310/bin/activate` 中写死的 `VIRTUAL_ENV=/datanvme/fuchengjia/projects/rabbitbot-dev-ros2/py310`。
- 已验证不挂载 `projectsjia` 的容器可用，挂载方式如下：

```bash
docker run -it \
  --name kuavo-agno-projects-only-test \
  --network host \
  -v /mnt/ssd/navgation/projects:/data \
  -v /mnt/ssd/navgation/projects:/datanvme/fuchengjia/projects \
  foxy-ros-cam-orb-ubuntu20:latest \
  bash
```

- 在上述容器内执行以下验证通过：

```bash
cd /data/rabbitbot-dev-ros2-master
source py310/bin/activate
python -c "import agno.workflow.v2; import cv2; import openai; print('imports ok')"
ls /datanvme/fuchengjia/projects/vln/ros2_ws/install/setup.bash
ls /datanvme/fuchengjia/projects/pyorbbecsdk-v2-py310
```

## 2026-04-28 STT async 接口兼容修复

- 现象：workflow 中持续调用 STT 异步接口时，STT 服务返回 `{"error":"'AudioToTextRecorder' object has no attribute 'set_global_prompt'"}`，上层无法解析，导致输入反复变成 `<REC_STOP>`。
- 原因：当前真实麦克风模式下使用的是 `RealtimeSTT.AudioToTextRecorder`，该对象没有项目代码假设的 `set_global_prompt()` 方法；无输入设备模式下的 `NoInputRecorder` 曾自带该方法，所以此前未暴露此问题。
- 修改文件：`/mnt/ssd/navgation/projects/rabbitbot-dev-ros2-master/stt_app.py`。
- 修改内容：`STTTimeoutWrapper.start()` 中调用 `set_global_prompt()` 前先判断 recorder 是否支持该方法；不支持时打印提示并跳过，不再返回错误。
- 同时增强 `STTTimeoutWrapper.stop_record()` 兼容性：调用 `set_enable_transcribe()` 和 `abort()` 前先判断 recorder 是否支持该方法，并将线程 join 改为带 `timeout=2`，避免 stop_async 因当前 RealtimeSTT 版本缺方法而报错或长时间阻塞。
- 语法验证：使用 `compile(open("stt_app.py").read(), "stt_app.py", "exec")` 检查通过。`python3 -m py_compile` 因远端 `__pycache__` 权限无法写入 `.pyc` 而失败，该失败与语法无关。
- 修复后需要重启 STT 服务才会生效：

```bash
docker exec navid-vllm-cuda-mic-audio bash -lc '
pkill -9 -f "uvicorn stt_app:app" 2>/dev/null || true
pkill -9 -f "scripts/start_stt_app.bash" 2>/dev/null || true
'

docker exec -d navid-vllm-cuda-mic-audio bash -lc '
cd /data/rabbitbot-dev-ros2-master
source /opt/venv/bin/activate

export LD_PRELOAD=/usr/lib/aarch64-linux-gnu/libgomp.so.1
export LD_LIBRARY_PATH=/opt/ctranslate2-cuda/lib:/usr/local/cuda/lib64:/usr/local/cuda/targets/aarch64-linux/lib:/usr/lib/aarch64-linux-gnu:${LD_LIBRARY_PATH}
export STT_DEVICE=cuda
export STT_COMPUTE_TYPE=float16
export INPUT_DEVICE_INDEX=26
export REALTIME_TTS_BASE_URL=http://localhost:28185/v1
export RABBITBOT_TTS_AGENT_URL=${REALTIME_TTS_BASE_URL}
export TORCH_HUB_DISABLE_NETWORK=1

uvicorn stt_app:app --host 0.0.0.0 --port 28184 --log-level debug --workers 1 \
  > /tmp/rabbitbot_stt.log 2>&1
'
```

- 验证命令：

```bash
curl -X POST http://127.0.0.1:28184/exec \
  -F 'task={"task": "start_async", "lang": "zh", "text": "", "timeout": 30}'
```

- 预期：不再返回 `AudioToTextRecorder object has no attribute set_global_prompt`。
